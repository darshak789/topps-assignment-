#include<stdio.h>
int main()
{
    char name[20];
    char age[20];
    char address[20];
    char city[20];
    char emali[20];

    printf("entre the name:\n");
    scanf("%s", name);
    gets(name);

    printf("entre the age:\n");
    scanf("%s", age);
    gets(age);

    printf("entre the address:\n");
    scanf("%s", address);
    gets(address);

    printf("entre the city:\n");
    scanf("%s", city);
    gets(city);

    printf("entre the emali:\n");
    scanf("%s", emali);
    gets(emali);
}